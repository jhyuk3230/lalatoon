export type HeaderNavItem = {
  text: string;
  link: string;
};

export type MyNavItem = {
  text: string;
  link: string;
};

export type tagStyleType = {
  new?: string;
  up?: string;
  end?: string;
};